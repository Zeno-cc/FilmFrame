{
  "name": "film-frame-maker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test:geometry": "tsc --noEmit --lib ES2020,DOM --module ESNext --moduleResolution Bundler --strict --skipLibCheck --types vite/client tests/filmGeometry.test.ts",
    "test:preview": "tsc --noEmit --lib ES2020,DOM --module ESNext --moduleResolution Bundler --strict --skipLibCheck --types vite/client tests/previewNavigation.test.ts",
    "test:settings": "tsc --noEmit --lib ES2020,DOM --module ESNext --moduleResolution Bundler --strict --skipLibCheck --types vite/client tests/settingsStorage.test.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "exif-js": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.4",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.12",
    "tailwindcss": "^4.2.4",
    "typescript": "^5.2.0",
    "vite": "^5.0.0"
  }
}
